import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
        primary: {
            main: '#574C4A',
            contrastText: '#edb8af',
        },
        secondary: {
            main: '#806F6C',
            contrastText: '#edb8af',
        },
    },
});

export default theme;
